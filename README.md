# Pinger

This application pings the ProConnect team whenever an incident is received via Uptime Robot

## Run

### En dev

`npm run startDev`

### En prod

`npm run build && npm run start`

## Migrations

Générer une migration :
`npm run migration:generate --name=nom-de-la-migration`

Run une migration :
`npm run migration:run`
